name: DriveStats
options:
  bundleIdPrefix: com.drivestats
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

packages:
  DriveStatsCore:
    path: Packages/DriveStatsCore

targets:
  DriveStats:
    type: application
    platform: iOS
    sources:
      - App
    dependencies:
      - package: DriveStatsCore
    info:
      path: App/Info.plist
      properties:
        CFBundleDisplayName: DriveStats
        UILaunchScreen: {}
        ITSAppUsesNonExemptEncryption: false
        NSLocationWhenInUseUsageDescription: DriveStats records your speed, distance, and route while you drive. Location is only used during an active recording.
        NSLocationAlwaysAndWhenInUseUsageDescription: Allowing location access at all times lets DriveStats detect and record drives automatically, even when the app is in the background.
        NSMotionUsageDescription: Motion activity is used to detect when a drive starts and ends so recording can happen automatically.
        UIBackgroundModes:
          - location
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.drivestats.app
        # Free personal team for local device installs; CI overrides signing off.
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: 24K92WVHMH
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "5.9"
    scheme:
      testTargets: []
